# Public values only. Never put tokens, passwords, or private keys in FE .env.
# Leave empty to use same-origin '/api' with Vite proxy in development.
# Example direct backend value: http://127.0.0.1:8081
VITE_API_BASE_URL=
